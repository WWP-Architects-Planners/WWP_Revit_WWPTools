<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copy Areas - Setup"
        Width="700"
        Height="760"
        MinWidth="620"
        MinHeight="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        FontSize="16">
    <Window.Resources>
        <Style x:Key="SwitchToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="19"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <Border x:Name="SwitchTrack"
                                    Background="#8D99A6"
                                    CornerRadius="9.5"/>
                            <Ellipse x:Name="SwitchThumb"
                                     Width="15"
                                     Height="15"
                                     Fill="#F3F5F7"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"
                                     Margin="2,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="#2F9BE8"/>
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="SwitchTrack" Property="Opacity" Value="0.45"/>
                                <Setter TargetName="SwitchThumb" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedButtonBase" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryRoundedButton" TargetType="Button" BasedOn="{StaticResource RoundedButtonBase}">
            <Setter Property="Background" Value="#1E66C5"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="SecondaryRoundedButton" TargetType="Button" BasedOn="{StaticResource RoundedButtonBase}">
            <Setter Property="Background" Value="#8A8F98"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Source area scheme:"
                   Margin="0,0,0,6"/>
        <ComboBox x:Name="SourceSchemeCombo"
                  Grid.Row="1"
                  Height="30"
                  Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2"
                    Margin="0,0,0,10">
            <TextBlock Text="Target area scheme:"
                       Margin="0,0,0,6"/>
            <ComboBox x:Name="TargetSchemeCombo"
                      Height="30"
                      Margin="0,0,0,10"/>

            <TextBlock Text="Levels to copy:"
                       Margin="0,0,0,6"/>
            <ListBox x:Name="LevelsList"
                     MinHeight="220"
                     SelectionMode="Extended"/>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Vertical"
                    Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center">
                <ToggleButton x:Name="TagUntaggedToggle"
                              Style="{StaticResource SwitchToggleStyle}"
                              IsChecked="True"
                              Margin="0,0,8,0"/>
                <TextBlock VerticalAlignment="Center"
                           Text="Tag all untagged areas in target views"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="0,8,0,0">
                <ToggleButton x:Name="CopyColorSchemeToggle"
                              Style="{StaticResource SwitchToggleStyle}"
                              IsChecked="False"
                              Margin="0,0,8,0"/>
                <TextBlock VerticalAlignment="Center"
                           Text="Copy area color schemes (source -&gt; target views)"/>
            </StackPanel>
        </StackPanel>

        <TextBlock x:Name="ValidationText"
                   Grid.Row="4"
                   Foreground="#AA1111"
                   Visibility="Collapsed"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Width="110"
                    Margin="0,0,8,0"
                    Style="{StaticResource PrimaryRoundedButton}"
                    Content="OK"/>
            <Button x:Name="CancelButton"
                    Width="110"
                    Style="{StaticResource SecondaryRoundedButton}"
                    Content="Cancel"/>
        </StackPanel>
    </Grid>
</Window>
