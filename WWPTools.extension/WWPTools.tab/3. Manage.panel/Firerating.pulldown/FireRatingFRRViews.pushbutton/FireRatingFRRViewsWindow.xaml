<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Create Fire Rating Lines — FRR Views"
    Width="560" Height="580"
    MinWidth="460" MinHeight="460"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResizeWithGrip"
    Background="#F6F8FB">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Create Fire Rating Lines — All FRR Views"
                       FontSize="20" FontWeight="SemiBold"
                       Foreground="#17324D" TextWrapping="Wrap"/>
            <TextBlock Text="Select views to process. Views with &#39;FRR&#39; in their name are listed."
                       FontSize="12" Foreground="#4F6478"
                       Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- FRR Param -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="FRR Parameter Name:"
                       VerticalAlignment="Center"
                       Foreground="#17324D" FontSize="13"/>
            <TextBox Grid.Column="1" x:Name="TxtFrrParam"
                     Height="32" Padding="8,4"
                     BorderBrush="#D9E2EC" BorderThickness="1"
                     FontSize="13" VerticalContentAlignment="Center"/>
        </Grid>

        <!-- Select/Deselect buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
            <Button x:Name="BtnSelectAll" Content="Select All"
                    Width="90" Height="28" FontSize="12"
                    Background="White" BorderBrush="#D9E2EC"
                    Foreground="#17324D" Margin="0,0,6,0" Cursor="Hand"/>
            <Button x:Name="BtnDeselectAll" Content="Deselect All"
                    Width="90" Height="28" FontSize="12"
                    Background="White" BorderBrush="#D9E2EC"
                    Foreground="#17324D" Cursor="Hand"/>
        </StackPanel>

        <!-- View list -->
        <Border Grid.Row="3" BorderBrush="#D9E2EC" BorderThickness="1"
                CornerRadius="4" Margin="0,0,0,10">
            <ListBox x:Name="LstViews"
                     SelectionMode="Multiple"
                     Background="White" BorderThickness="0"
                     FontSize="13" Foreground="#17324D"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </Border>

        <!-- Status -->
        <TextBlock Grid.Row="5" x:Name="LblStatus"
                   Foreground="#B42318" FontSize="12"
                   Margin="0,4,0,8" TextWrapping="Wrap"/>

        <!-- Buttons -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" x:Name="BtnCancel"
                    Content="Cancel" Width="90" Height="34"
                    FontSize="13"
                    Background="White" BorderBrush="#D9E2EC"
                    Foreground="#17324D" Cursor="Hand"/>
            <Button Grid.Column="3" x:Name="BtnRun"
                    Content="Create Lines" Width="110" Height="34"
                    FontSize="13" FontWeight="SemiBold"
                    Background="#17324D" Foreground="White"
                    BorderThickness="0" Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>
