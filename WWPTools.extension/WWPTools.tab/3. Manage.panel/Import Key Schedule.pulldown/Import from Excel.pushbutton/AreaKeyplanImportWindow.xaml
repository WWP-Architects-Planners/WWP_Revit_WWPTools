<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Area Key Schedule"
        Height="720"
        Width="980"
        MinHeight="560"
        MinWidth="820"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Background="#FFF7F8FA">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#FFD9DEE7" />
        <SolidColorBrush x:Key="TitleBrush" Color="#FF152033" />
        <SolidColorBrush x:Key="MutedBrush" Color="#FF5F6B7A" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF175CD3" />
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#FF1849A9" />
        <SolidColorBrush x:Key="PrimaryPressedBrush" Color="#FF12387A" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="SecondaryBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="SecondaryBorderBrush" Color="#FFC5CCD8" />
        <SolidColorBrush x:Key="SecondaryHoverBrush" Color="#FFF3F5F8" />
        <SolidColorBrush x:Key="SecondaryPressedBrush" Color="#FFE7EBF1" />

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="BorderBrush" Value="#FFC8D0DB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="BorderBrush" Value="#FFC8D0DB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="MinHeight" Value="30" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryPressedBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryPressedBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Foreground" Value="{StaticResource TitleBrush}" />
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBorderBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryPressedBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18"
                Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="Import Area Key Schedule"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TitleBrush}" />
                <TextBlock Text="Choose an Excel file, load the source columns, then map each column to a Revit parameter."
                           Margin="0,8,0,0"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource MutedBrush}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="18"
                Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           Text="Excel File"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TitleBrush}"
                           Margin="0,0,0,6" />
                <TextBlock Grid.Row="0"
                           Grid.Column="1"
                           Text="Target Type"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TitleBrush}"
                           Margin="14,0,0,6" />

                <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,14,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="FilePathBox"
                             Grid.Column="0"
                             MinHeight="32" />
                    <Button x:Name="BrowseButton"
                            Grid.Column="1"
                            Content="Browse"
                            Margin="8,0,0,0"
                            Style="{StaticResource SecondaryButtonStyle}" />
                    <Button x:Name="LoadButton"
                            Grid.Column="2"
                            Content="Load"
                            Margin="8,0,0,0"
                            Style="{StaticResource PrimaryButtonStyle}" />
                </Grid>

                <ComboBox x:Name="TargetTypeCombo"
                          Grid.Row="1"
                          Grid.Column="1"
                          Width="240"
                          MinHeight="32" />
            </Grid>
        </Border>

        <TextBlock Grid.Row="2"
                   Text="Map Excel columns to parameters"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource MutedBrush}"
                   Margin="0,0,0,8" />

        <DataGrid x:Name="MappingGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  HeadersVisibility="Column"
                  VirtualizingPanel.IsVirtualizing="False"
                  Margin="0,0,0,12"
                  MinHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Excel Column"
                                    Binding="{Binding ColumnName}"
                                    IsReadOnly="True"
                                    Width="2*" />
                        <DataGridTemplateColumn Header="Map To"
                                                Width="3*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding ElementName=MappingGrid, Path=Tag}"
                                              SelectedItem="{Binding SelectedOption, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="4">
            <Image x:Name="LogoImage"
                   DockPanel.Dock="Left"
                   Width="56"
                   Height="56"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Left"
                   IsHitTestVisible="False" />
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Style="{StaticResource SecondaryButtonStyle}" />
                <Button x:Name="OkButton"
                        Content="OK"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
