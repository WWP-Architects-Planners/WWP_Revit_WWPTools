<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copy Color Scheme"
        Width="720"
        Height="420"
        MinWidth="620"
        MinHeight="360"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        FontFamily="Segoe UI"
        FontSize="14"
        Background="#F3F5F7">
    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3F9AD9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F9AD9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="26.5"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2E87C5"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#2E87C5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Border Background="#FFFFFF"
                BorderBrush="#D7DEE6"
                BorderThickness="1"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Source color scheme"
                           Margin="0,0,0,6"
                           Foreground="#6B7280"
                           FontWeight="SemiBold"/>
                <ComboBox x:Name="SourceSchemeCombo" Grid.Row="1" Margin="0,0,0,12"/>

                <TextBlock Grid.Row="2"
                           Text="Target color scheme"
                           Margin="0,0,0,6"
                           Foreground="#6B7280"
                           FontWeight="SemiBold"/>
                <ComboBox x:Name="TargetSchemeCombo" Grid.Row="3" Margin="0,0,0,12"/>

                <TextBlock x:Name="ValidationText"
                           Grid.Row="4"
                           Foreground="#C43D3D"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8"
                           FontWeight="SemiBold"/>

                <StackPanel Grid.Row="5">
                    <CheckBox x:Name="CreateNewSchemeCheck"
                              Margin="0,0,0,10"
                              Content="Create new scheme in target scope (leave selected target unchanged)" />

                    <TextBlock Foreground="#6B7280"
                               FontSize="13"
                               TextWrapping="Wrap"
                               Text="Unchecked: overwrites the selected target scheme. Checked: creates a new scheme in the selected target scope and copies into it."/>
                </StackPanel>

                <StackPanel Grid.Row="6"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="OkButton"
                            Width="110"
                            Margin="0,0,8,0"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Content="OK"/>
                    <Button x:Name="CancelButton"
                            Width="110"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Content="Cancel"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
