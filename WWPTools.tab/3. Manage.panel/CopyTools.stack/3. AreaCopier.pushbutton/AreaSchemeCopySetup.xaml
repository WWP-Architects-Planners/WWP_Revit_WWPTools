<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copy Areas - Setup"
        Width="700"
        Height="760"
        MinWidth="620"
        MinHeight="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        FontFamily="Segoe UI"
        FontSize="14"
        Background="#F3F5F7">
    <Window.Resources>
        <Style x:Key="FlatToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="19"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <Border x:Name="SwitchTrack"
                                    Background="#B8C1CC"
                                    BorderBrush="#AEB8C4"
                                    BorderThickness="1"
                                    CornerRadius="9.5"/>
                            <Ellipse x:Name="SwitchThumb"
                                     Width="15"
                                     Height="15"
                                     Fill="White"
                                     Stroke="#AEB8C4"
                                     StrokeThickness="1"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"
                                     Margin="2,2,21,2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="#3F9AD9"/>
                                <Setter TargetName="SwitchTrack" Property="BorderBrush" Value="#3F9AD9"/>
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Left"/>
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="21,2,2,2"/>
                                <Setter TargetName="SwitchThumb" Property="Stroke" Value="#3F9AD9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="SwitchTrack" Property="Opacity" Value="0.45"/>
                                <Setter TargetName="SwitchThumb" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3F9AD9"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F9AD9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="26.5"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2E87C5"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#2E87C5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Border Background="#FFFFFF"
                BorderBrush="#D7DEE6"
                BorderThickness="1"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Source area scheme"
                           Margin="0,0,0,6"
                           Foreground="#6B7280"
                           FontWeight="SemiBold"/>
                <ComboBox x:Name="SourceSchemeCombo"
                          Grid.Row="1"
                          Margin="0,0,0,12"/>

                <Grid Grid.Row="2" Margin="0,0,0,12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Target area scheme"
                               Margin="0,0,0,6"
                               Foreground="#6B7280"
                               FontWeight="SemiBold"/>
                    <ComboBox x:Name="TargetSchemeCombo"
                              Grid.Row="1"
                              Margin="0,0,0,12"/>

                    <TextBlock Grid.Row="2"
                               Text="Levels to copy"
                               Margin="0,0,0,6"
                               Foreground="#6B7280"
                               FontWeight="SemiBold"/>
                    <ListBox x:Name="LevelsList"
                             Grid.Row="3"
                             MinHeight="220"
                             SelectionMode="Extended"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
                </Grid>

                <StackPanel Grid.Row="3" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <ToggleButton x:Name="TagUntaggedToggle"
                                      Style="{StaticResource FlatToggleStyle}"
                                      IsChecked="True"
                                      Margin="0,0,10,0"/>
                        <TextBlock VerticalAlignment="Center"
                                   Text="Tag all untagged areas in target views"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8,0,0">
                        <ToggleButton x:Name="CopyColorSchemeToggle"
                                      Style="{StaticResource FlatToggleStyle}"
                                      IsChecked="False"
                                      Margin="0,0,10,0"/>
                        <TextBlock VerticalAlignment="Center"
                                   Text="Copy area color schemes (source to target views)"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock x:Name="ValidationText"
                           Grid.Row="4"
                           Foreground="#C43D3D"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"
                           Margin="0,0,0,8"
                           FontWeight="SemiBold"/>

                <DockPanel Grid.Row="5">
                    <Image x:Name="LogoImage"
                           DockPanel.Dock="Left"
                           Width="56"
                           Height="56"
                           VerticalAlignment="Bottom"
                           HorizontalAlignment="Left"
                           IsHitTestVisible="False" />
                    <StackPanel DockPanel.Dock="Right"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button x:Name="OkButton"
                                Width="110"
                                Margin="0,0,8,0"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Content="OK"/>
                        <Button x:Name="CancelButton"
                                Width="110"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Content="Cancel"/>
                    </StackPanel>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
